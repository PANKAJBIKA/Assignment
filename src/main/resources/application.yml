spring:
  application:
    name: Assignment-1

  datasource:
    url: jdbc:mysql://localhost:3306/workspace
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  data:
    redis:
      host: localhost
      port: 6379

  kafka:
    bootstrap-servers: localhost:9092

server:
  port: 8080
